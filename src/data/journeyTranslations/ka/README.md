# Georgian journey translations

Mirror of `../de/` — one file per pathId, default-exporting
`{ title, subtitle, description, days: { <dayNumber>: { title, theme, prompt } } }`.
Files and individual keys are optional; anything missing falls back to English.
